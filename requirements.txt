pandas>=1.5.0
numpy>=1.21.0
scipy>=1.9.0
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.0.0
xgboost>=1.7.0
lightgbm>=3.3.0
catboost>=1.1.0
lime>=0.2.0
shap>=0.41.0
tqdm>=4.64.0
joblib>=1.1.0

# Structural feature engineering (script 04 only) - biopython is core to the method
# (AlphaFold SASA via Bio.PDB), guarded by a try/except in code.
biopython>=1.81

# NOTE: not currently imported anywhere in src/ as of this pipeline version.
# Kept in case upcoming topological-feature work depends on them; prune if unused.
giotto-tda>=0.6.0
pyfaidx>=0.7.2
psutil>=5.9.0

# ESM-2 embeddings + Cross-Attention fusion net (scripts 10, 11, 12; common.py).
# A CUDA build of torch is strongly recommended for stage 10 (FP16 auto-enabled).
torch>=2.0.0
fair-esm>=2.0.0
